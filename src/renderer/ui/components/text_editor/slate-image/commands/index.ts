import { insertImage } from './insertImage';
import {
  setImageSize,
  resetOriginalSize,
  setImageSizeByRatio,
} from './setImageSize';

export default {
  setImageSize,
  resetOriginalSize,
  setImageSizeByRatio,
  insertImage,
};
